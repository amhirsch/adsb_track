# ADS-B Track
Python logging utility for ADS-B receivers.

## Installation Setup
1. Clone repository and navigate to directory
2. run `$python3 -m pipenv install --dev && python3 -m pipenv run pip install -e .`

## Database
### Views
* ident_recent
* position_recent
* velocity_recent
